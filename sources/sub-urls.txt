# Clash / base64 / plain subscription URLs (one per line).
# Prefer raw.githubusercontent.com or stable public feeds you trust.
# Leave empty if you only want the pool track online; fill after you collect sources.
